const name=require('http')

const server=name.createServer();
server.listen(3000);
console.log('listening on port 3000...')

///const http=require('http')

//const server=http.createServer();
//server.listen(4000);
//console.log('listening on port 4000...')